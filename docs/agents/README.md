# docs/agents — Agent 文档索引

本目录存放项目结构化文档，供 agent 按需读取。

| 文件 | 关键词 | 何时读取 |
|------|--------|----------|
| `architecture.md` | 架构、目录结构、核心引擎、分层 | 需要了解项目代码布局时 |
| `cli.md` | CLI 命令、队列管理、GitHub 推送、模型选择 | 需要查询命令用法或模型参数时 |
| `workflow.md` | 转录流程、降级策略、注意事项 | 需要了解转录流程或环境配置时 |
| `memory.md` | 内存管理、mmap、Swap、卡死分析 | 需要排查 Whisper 内存/卡死问题时 |
| `scheduling.md` | 调度策略、CPU 感知、内存需求 | 需要了解队列调度规则或模型内存需求时 |
| `output.md` | 输出目录、out、文件结构 | 需要了解转录结果存放位置时 |